<UserControl x:Class="PvzLauncherRemake.Controls.UserTutorialHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PvzLauncherRemake.Controls"
             xmlns:icons="clr-namespace:PvzLauncherRemake.Controls.Icons"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!--MASK-->
        <Path Fill="Black" Opacity="0.5">
            <Path.Data>
                <CombinedGeometry GeometryCombineMode="Exclude">
                    <CombinedGeometry.Geometry1>
                        <RectangleGeometry x:Name="geometry_mask"/>
                    </CombinedGeometry.Geometry1>
                    <CombinedGeometry.Geometry2>
                        <RectangleGeometry x:Name="geometry_hole"/>
                    </CombinedGeometry.Geometry2>
                </CombinedGeometry>
            </Path.Data>
        </Path>

        <!--Tutorial Card-->
        <Grid x:Name="grid_tuCard" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border CornerRadius="5" Background="{DynamicResource TUCardBackground}" BorderThickness="2" BorderBrush="{DynamicResource TUCardBorder}"/>
            <Grid Margin="10">
                <icons:IconTeacher Height="80" Width="80" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Margin="90,0,0,0">
                    <TextBlock x:Name="textBlock_cardTitle" Text="Title" TextWrapping="Wrap" FontWeight="Bold" FontSize="20"/>
                    <TextBlock x:Name="textBlock_cardContent" Text="Content" TextWrapping="Wrap" Margin="0,5,0,0"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,10,0,0">
                        <Button x:Name="button_exitTutorial" Content="退出教程"/>
                        <Button x:Name="button_nextTutorial" Content="下一步" Style="{DynamicResource AccentButtonStyle}" Margin="10,0,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</UserControl>

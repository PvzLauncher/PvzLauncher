<ui:Page x:Class="PvzLauncherRemake.Pages.PageTask"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PvzLauncherRemake.Pages"
      xmlns:ui="http://schemas.modernwpf.com/2019"
      xmlns:controls="clr-namespace:PvzLauncherRemake.Controls"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageTask"
         
         
      xmlns:lex="http://wpflocalizeextension.codeplex.com"
      lex:ResxLocalizationProvider.DefaultAssembly="PvzLauncherRemake"
      lex:ResxLocalizationProvider.DefaultDictionary="I18N.PageTask">

    <Grid>
        <Grid>
            <!--上栏-->
            <Border BorderThickness="1.5" CornerRadius="5" VerticalAlignment="Top" Margin="5" Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}">

                <Grid Margin="10" VerticalAlignment="Top" >
                    <TextBlock x:Name="textBlock_ProgressAverage" Text="{lex:Loc Total_Progress}" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,0,0,10"/>
                    <TextBlock x:Name="textBlock_SpeedSum" Text="{lex:Loc Total_Speed}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,0,0,10"/>
                    <ProgressBar x:Name="progressBar_Average" Background="#19000000" VerticalAlignment="Bottom"/>
                </Grid>
            </Border>

            <!--任务列表-->
            <controls:UserScrollViewer Margin="0,60,0,0">
                <StackPanel>
                    <DockPanel>
                        <TextBlock Text="{lex:Loc InProgress}" Opacity="0.5" Margin="10" DockPanel.Dock="Left"/>
                        <Rectangle Fill="Black" Opacity="0.2" VerticalAlignment="Center" MinHeight="2"/>
                    </DockPanel>
                    <StackPanel x:Name="stackPanel_Tasks" Background="#00000000" Margin="15,0,0,0"/>
                    <DockPanel>
                        <TextBlock Text="{lex:Loc Completed}" Opacity="0.5" Margin="10" DockPanel.Dock="Left"/>
                        <Rectangle Fill="Black" Opacity="0.2" VerticalAlignment="Center" MinHeight="2"/>
                    </DockPanel>
                    <StackPanel x:Name="stackPanel_Completed" Background="#00000000" Margin="15,0,0,0"/>
                </StackPanel>
            </controls:UserScrollViewer>
        </Grid>

        
    </Grid>
</ui:Page>

<ui:Page x:Class="PvzLauncherRemake.Pages.PageToolbox"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PvzLauncherRemake.Pages"
      xmlns:ui="http://schemas.modernwpf.com/2019"
      xmlns:ctrl="clr-namespace:PvzLauncherRemake.Controls"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageToolbox"
         
      xmlns:lex="http://wpflocalizeextension.codeplex.com"
      lex:ResxLocalizationProvider.DefaultAssembly="PvzLauncherRemake"
      lex:ResxLocalizationProvider.DefaultDictionary="I18N.PageToolbox">

    <Grid>
        <ctrl:UserScrollViewer>
            <StackPanel Margin="10">
                <!--Pak操作-->
                <Grid Margin="0,0,0,10">
                    <Border BorderThickness="2" CornerRadius="3" Background="#19FFFFFF">
                        <Border.BorderBrush>
                            <LinearGradientBrush EndPoint="0,3" MappingMode="Absolute">
                                <LinearGradientBrush.RelativeTransform>
                                    <ScaleTransform CenterY="0.5" ScaleY="-1"/>
                                </LinearGradientBrush.RelativeTransform>
                                <GradientStop Color="#33000000"/>
                                <GradientStop Color="#19000000" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.BorderBrush>
                    </Border>


                    <StackPanel Margin="10">
                        <TextBlock Text="{lex:Loc PakOperation}" Margin="0,0,0,10" FontSize="20" FontWeight="Bold"/>


                        <!--Pak-->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <TextBlock Text="Pak:" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <TextBox x:Name="textBox_Pak_Pak" IsReadOnly="True" Margin="0,0,10,0"/>
                            <Button x:Name="button_Pak_Pak_Broswer" Content="{lex:Loc Broswe}"/>
                        </StackPanel>
                        <!--文件夹-->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <TextBlock Text="{lex:Loc TargetLocation}" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <TextBox x:Name="textBox_Pak_Directory" IsReadOnly="True" Margin="0,0,10,0"/>
                            <Button x:Name="button_Pak_Directory_Broswer" Content="{lex:Loc Broswe}"/>
                        </StackPanel>
                        <!--操作-->
                        <StackPanel Orientation="Horizontal">
                            <Button x:Name="button_Pak_Pack" Content="{lex:Loc Pack}" Margin="0,0,5,0"/>
                            <Button x:Name="button_Pak_Unpack" Content="{lex:Loc Unpack}" Margin="0,0,5,0"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </ctrl:UserScrollViewer>
    </Grid>
</ui:Page>

<ui:Page x:Class="PvzLauncherRemake.Pages.PageLaunch"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PvzLauncherRemake.Pages"
      xmlns:ui="http://schemas.modernwpf.com/2019"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageLaunch"
         
         
      xmlns:lex="http://wpflocalizeextension.codeplex.com"
      lex:ResxLocalizationProvider.DefaultAssembly="PvzLauncherRemake"
      lex:ResxLocalizationProvider.DefaultDictionary="I18N.PageLaunch">

    <Grid>
        <!--背景图-->
        <Image x:Name="image" Stretch="UniformToFill" Opacity="0.7" Margin="-20,-20,-20,-20">
            <Image.Effect>
                <BlurEffect Radius="25"/>
            </Image.Effect>
        </Image>
        
        
        <Grid Margin="10,10,10,10">
            <!--启动按钮-->
            <StackPanel x:Name="stackPanel_LaunchButtons" VerticalAlignment="Bottom" HorizontalAlignment="Right">
                <!--启动游戏-->
                <Button x:Name="button_Launch" Style="{DynamicResource AccentButtonStyle}" Height="60" Width="250" Margin="0,0,0,5" Click="button_Launch_Click">
                    <StackPanel>
                        <TextBlock x:Name="textBlock_LaunchText" Text="{lex:Loc LaunchGame}" Foreground="#E4FFFFFF" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="textBlock_LaunchVersion" Text="Version" FontSize="10" HorizontalAlignment="Center" Foreground="#7FFFFFFF"/>
                    </StackPanel>
                </Button>

                <!--启动修改器-->
                <Button x:Name="button_LaunchTrainer" Width="250" Height="30" Click="button_LaunchTrainer_Click">
                    <StackPanel>
                        <TextBlock Text="{lex:Loc LaunchTrainer}"/>
                    </StackPanel>
                </Button>
            </StackPanel>

            <!--标题-->
            <Viewbox x:Name="viewBox_Icon" VerticalAlignment="Top" HorizontalAlignment="Center" Height="60" Margin="0,10,0,0"/>

            <!--回声洞-->
            <Button x:Name="button_EchoCave" Opacity="0" VerticalAlignment="Bottom" Background="{x:Null}" BorderBrush="{x:Null}"/>
        </Grid>
    </Grid>
</ui:Page>

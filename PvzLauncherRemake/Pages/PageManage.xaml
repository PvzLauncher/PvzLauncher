<ui:Page x:Class="PvzLauncherRemake.Pages.PageManage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PvzLauncherRemake.Pages"
      xmlns:ui="http://schemas.modernwpf.com/2019"
      xmlns:ctrl="clr-namespace:PvzLauncherRemake.Controls"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageManage" Loaded="Page_Loaded"
         
      xmlns:lex="http://wpflocalizeextension.codeplex.com"
      lex:ResxLocalizationProvider.DefaultAssembly="PvzLauncherRemake"
      lex:ResxLocalizationProvider.DefaultDictionary="I18N.PageManage">

    <Grid>
        <Grid x:Name="grid_Main">
            <TabControl x:Name="tabControl" Style="{StaticResource TabControlPivotStyle}" SelectionChanged="tabControl_SelectionChanged">
                <!--游戏管理-->
                <TabItem Header="{lex:Loc GameManage}">
                    <Grid>
                        <ctrl:UserScrollViewer>
                            <StackPanel x:Name="stackPanel_Game" Background="#00000000" Margin="0,0,0,10"/>
                        </ctrl:UserScrollViewer>
                    </Grid>
                </TabItem>
                <!--修改器管理-->
                <TabItem Header="{lex:Loc TrainerManage}">
                    <Grid>
                        <ctrl:UserScrollViewer>
                            <StackPanel x:Name="stackPanel_Trainer" Background="#00000000" Margin="0,0,0,10"/>
                        </ctrl:UserScrollViewer>
                    </Grid>
                </TabItem>
                <!--搜索-->
                <TabItem Header="{lex:Loc Search}">
                    <Grid>
                        <Grid VerticalAlignment="Top">
                            <TextBox x:Name="textBox_Search" Margin="0,0,110,0" KeyUp="textBox_Search_KeyUp"/>
                            <Button x:Name="button_Search" Style="{DynamicResource AccentButtonStyle}" Content="{lex:Loc Search}" MinWidth="100" HorizontalAlignment="Right" Click="button_Search_Click"/>
                        </Grid>

                        <ctrl:UserScrollViewer Margin="0,42,0,0">
                            <StackPanel x:Name="stackPanel_Search"/>
                        </ctrl:UserScrollViewer>
                    </Grid>
                </TabItem>
            </TabControl>

            <!--导入游戏按钮-->
            <Button x:Name="button_ImportGame" Content="{lex:Loc ImportGame}" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,10,10,0" Click="button_ImportGame_Click"/>


        </Grid>


        <!--加载屏幕-->
        <Grid x:Name="grid_Loading" Visibility="Hidden">
            <ui:ProgressRing IsActive="True" Width="50" Height="50"/>
            <TextBlock x:Name="textBlock_Loading" Text="加载中..." VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,100,0,0"/>
        </Grid>

    </Grid>
</ui:Page>
